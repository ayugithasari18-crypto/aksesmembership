/** @type {import('next').NextConfig} */
const nextConfig = {
    // Memastikan trailing slash dipertahankan, penting untuk routing yang konsisten
    trailingSlash: true,
};

module.exports = nextConfig;
